{
  "name": "jesse-keller-website",
  "type": "module",
  "version": "1.0.0",
  "description": "Website von Jesse Keller - Unterlagen und Software",
  "scripts": {
    "dev": "astro dev --host",
    "build:music": "mkdir -p public/musik && for f in music-sources/*.ly; do lilypond -o \"public/musik/$(basename \"${f%.ly}\")\" \"$f\"; done",
    "build": "npm run build:music && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^5.1.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "wrangler": "^4.0.0"
  }
}
